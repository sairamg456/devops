---
- name: install tomcat server
  hosts: webserver
  vars:
  java_package: openjdk-11-jdk
  become: true
  tasks:
    - name: Install Java
      ansible.builtin.apt:
        name: "{{ java_package }}"
        update_cache: yes
        state: present

      

        
