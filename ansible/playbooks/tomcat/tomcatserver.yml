---
- name: install tomcat server
  hosts: webserver
  become: true
  tasks:
    - name: Install Java openjdk-11-jdk
      ansible.builtin.apt:
        name: "{{ java_package }}"
        update_cache: yes
        state: present

      

        
